{
  "name": "mathmint-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for MathMint Quiz on Mezo L2",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:mezo": "hardhat run scripts/deploy.js --network mezo"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.0.0"
  }
}